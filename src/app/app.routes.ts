import { Routes } from '@angular/router';
import { UserTableComponent } from '@app/features/users/user-table.component';

export const routes: Routes = [
  { path: '', component: UserTableComponent }
];
